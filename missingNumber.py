class Solution(object):
    def missingNumber(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        x = range(len(nums)+1)
        # lookFor = sum(x)
        # actual = sum(nums)
        # return lookFor - actual
        for i in range(1,len(x)):
            try:
                result = result
        
        return len(nums)